model:
  backbone: dinov3
  weights: weights/pretrained/spine_dinov3_vitb16_teacher_20000.pth
  image_size: 512
  freeze_backbone: true
training:
  batch_size: 4
  epochs: 100
  learning_rate: 0.001
  weight_decay: 0.0
  workers: 4
  seed: 42
  early_stopping:
    enabled: true
    patience: 20
    min_delta: 0.0
output_dir: outputs/downstream/task09_linear/spine_dinov3_teacher_20k
