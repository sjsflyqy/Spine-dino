model:
  backbone: dinov2
  weights: weights/pretrained/spine_rad_dino_maira2_vitb14_teacher_20000.pth
  image_height: 896
  image_width: 448
training:
  batch_size: 2
  epochs: 100
  learning_rate: 0.001
  weight_decay: 0.0
  heatmap_sigma: 1.5
  workers: 4
  seed: 42
  early_stopping:
    enabled: true
    patience: 20
    min_delta: 0.0001
output_dir: outputs/downstream/task07_keypoint_linear/spine_rad_dino_maira2_teacher_20k
