# Task 03: VinDr-SpineXR official test set

This directory contains the official 2,077 test PNG files, `test.csv`, and the
dataset license. Train images are deliberately excluded because they belong to
Spine-DINO pretraining.

The task is multi-label classification/detection. `No finding` is not an
ordinary mutually exclusive eighth softmax class.
